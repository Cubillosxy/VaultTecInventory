FROM node:20
WORKDIR /usr/src/app

# Build-time install (optional speed-up for CI builds)
# COPY package.json package-lock.json ./
# RUN npm ci

# We rely on docker-compose to run "npm install" at startup,
# because the host bind mount hides node_modules from image layers.
EXPOSE 3000
